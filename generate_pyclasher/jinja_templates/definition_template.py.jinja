"""
``{{ class_name }}`` class
"""

{% for module, module_values in sorted(imports.items()) %}from {{ module_values['import_level'] * '.' }}{{ module }} import {{ ", ".join(sorted(module_values['imports'])) }}
{% endfor %}

@ModelWrapper(primary_attributes={{ primary_attributes }}, exclude_annotations={{ exclude_annotations }})
class {{ class_name }}{% if parents %}({{ ', '.join(parents) }}){% endif %}:
    {% if annotations %}{% for annotation in annotations %}{{ annotation['name'] }}: {{ annotation['type'] }}
    {% endfor %}{% else %}pass

{% endif %}
